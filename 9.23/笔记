数组对象
 
 concat 链接数组
 arr.concat([1,2,3])
[1, 2, "a", "b", "c", 6, 7, 8, 9, 1, 2, 3]

 join（"分隔符"） 按照指定的分隔符将数组分开成字符串
 split（"分隔符"）与之相反

pop push  将数组当做栈
弹出 会压如 栈顶元素

shift  unshift 对头部 移出和添加元素

slice（）类似字符串截取

sort（[函数]）对数组进行排序，没有函数 默认排序（代码字母） 有函数 按照函数排序 函数为 回调函数

reverse 颠倒数组中元素


asplice(起始位置,[个数,替换元素1，替换元素2]) 删除移出替换元素。 会返回一个由被删除元素组成的的 数组，并且元素会被修改为替换后
var arr=[1,2,3,4,5,6,7,8,9]
undefined
arr.splice(2,3,'a','b','c')
[3, 4, 5]
arr
[1, 2, "a", "b", "c", 6, 7, 8, 9]
